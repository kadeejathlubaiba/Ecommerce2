// import { object } from "yup";
// import { Formik } from "formik";
// import * as Yup from "yup";

//  export const ValidatePassword =(props,condition) => {
//     const classes=useStyle(defaultClasses, props.classes)
//     if (props && props.touched && props.errors.passwords && condition){
//     if(
//         object.keys(props.errors).length >0 && props.errors.passwords.includes(condition)
//     )
//     {
//         return classes;
//     }
//     else if(props && props.touched && object.keys(props.errors).length ===0 && props.values.passwords.length > 0)
//     {
//         return classes.active;
//     }
//     else if(props && props.touched && object.keys(props.errors).length ===0 && props.values.passwords.length > 0)
//     {
//         return classes.active;
//     }
// }